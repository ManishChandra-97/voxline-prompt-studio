{
  "name": "voxline-prompt-studio",
  "version": "1.0.0",
  "private": true,
  "description": "Local-first workspace for designing and versioning safe voice AI prompts.",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "node scripts/build.mjs",
    "preview": "node server.js --dist",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check app.js && npm run test",
    "push": "npm run check && npm run build && git push"
  },
  "engines": {
    "node": ">=18"
  }
}
